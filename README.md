# マイリワードポータル プロトタイプ

ロイヤリティアプリのUIプロトタイプ(フレームワーク不使用)。モジュール単位でクライアントに合わせて機能を選べる、というビジネス展開を想定し、3つの構成を用意している。

## 3種類のビルド

- **`index.html`(フル版)** — https://ayumunomoto.github.io/myreward-portal-demo/
  通常/スポーツ観光/わかおしっ！の3モード切替、ガチャ、キャラクター演出・ストーリーゲージ、QR決済・チェックイン、リワード交換(カテゴリ別・共通ポイント交換)、アンケートチャレンジ、プッシュ通知デモ、利用履歴。下部ナビで各画面を行き来する。
- **`lite.html`(コア版)** — https://ayumunomoto.github.io/myreward-portal-demo/lite.html
  モードを1本化し、以下6モジュールのみに絞った構成:
  ポイント基盤(残高カード+履歴) / リワード交換(カテゴリ別・共通ポイント交換) / QR決済・チェックイン / チャレンジ(アンケート含む) / プッシュ通知。下部ナビで各画面を行き来する。
- **`hub.html`(ハブ版)** — https://ayumunomoto.github.io/myreward-portal-demo/hub.html
  コア版と同じ6モジュール構成だが、下部ナビを置かず、ホーム画面(残高カード)から必要な画面へ遷移する構成。残高カード内に「QRを読む」ボタンと「履歴を見る」ボタン、チャレンジ/リワードは各プロモカードの「すべて見る」から遷移する。各画面には戻るボタンを設置。

## 管理画面(CMS)プロトタイプ

- **`admin.html`** — https://ayumunomoto.github.io/myreward-portal-demo/admin.html
  上記アプリ向けの「アンケート」「プッシュ通知」を作成・配信・実績分析する運用者向け管理画面。デスクトップのダッシュボードUI(サイドバー+テーブル+フォーム)で、消費者向けアプリの3ビルドとはコードを共有していない独立したプロトタイプ。
  - アンケート管理: 質問設計(選択式、追加/削除可)、報酬ポイント設定、対象セグメント/配信チャネル/配信期間の設定、リアルタイムプレビュー、回答結果の設問別グラフ表示。
  - プッシュ通知管理: タイトル・本文・画像(プリセットのライブラリから選択)・リンク先の設定、即時配信/日時予約、消費者アプリの通知カードを再現したリアルタイムプレビュー、配信後の開封率/タップ率などの実績ファネル。
  - **広告(タイアップ)出稿を想定した設計**: 両方の作成フォームに「広告として配信」トグルがあり、オンにすると広告主名・出稿予算・目標件数(またはフリークエンシーキャップ)の入力欄が表示される。広告として配信する場合は下書き保存後に「審査へ提出」となり、一覧画面の「審査中」ステータスから運用者が内容を確認して承認(→配信)する簡易フローを組み込んでいる。ダッシュボードには広告出稿件数・出稿予算合計のKPI、結果画面には広告の場合のみ回答単価/タップ単価(出稿予算÷実績)を表示する。
  - **QRコード管理**: ポイント付与QR(チェックイン用)・ポイント利用QR(お支払い用)を発行・編集・有効/無効切替できる。管理項目はQRID(自動採番、種別に応じて `QR-B-######` / `QR-P-######`)、発行日、QR名称、ポイント数(種別に応じて「付与ポイント数」「利用上限ポイント数」とラベルが変わる)、有効期間(開始日・終了日)、設置場所、有効/無効、QR画像(Canvasで生成する疑似QRパターン、PNGダウンロード可)。有効期間切れは「無効」トグルとは別に「期限切れ」として自動判定・表示される。

## MASH向け リワード体験マネジメント コンソール(提案プロトタイプ)

- **`mash.html`** — https://ayumunomoto.github.io/myreward-portal-demo/mash.html
  株式会社マッシュホールディングス(MASH ID: gelato pique / Cosme Kitchen / SNIDEL / Biople 等のグループ共通会員基盤)向けの提案用プロトタイプ。「ECで貯まったポイントを、割引消化ではなくリアル店舗の接客体験へ転換する」ためのリワード管理SaaSを想定した運用者向け画面。上記のマイリワードポータル系3ビルドおよび `admin.html` とはコードを共有していない独立したファイル。

  サイドバー上部の**プロトタイプ切替**で、2つの構成を行き来できる(切り替えるとアクセントカラー・ナビゲーション・ロール表示が変わり、共創型でのみ増える機能には「拡張 / 新設」バッジが付く)。

  ### 1. 自社完結型(ベースライン) — MASH CORE CLOUD
  企画例: Cosme Kitchen「パーソナル肌診断＆カスタムハーブティー処方体験」

  | 画面 | 対象ロール | 内容 |
  | --- | --- | --- |
  | 1-A 自社エクスペリエンス作成 | 本部マーケ | 基本設定 / 交換レート・会員ゲート(ランク別公開と推定リーチ) / 店舗フルフィルメント(展開店舗・接客枠・手渡し部材・在庫連動と在庫ゼロ時の挙動) / 来店消込◯日後のフォローアップ自動配信。右カラムに会員アプリの交換画面とプッシュ通知のリアルタイムプレビュー、公開前チェック(未入力・期間逆転・在庫連動オフなどを判定し、NGなら公開ボタンを無効化) |
  | 1-B 店頭消込＆カルテ | 店頭スタッフ | 本日の予約一覧 → 会員バーコード読取 → カウンセリング記録(肌悩み・処方ハーブ最大3種・推奨アイテム)→ MASH IDへ保存。認証結果は「認証成功 / 有効期限切れ / 別店舗の予約 / 消込済み」をデモ操作で切り替えられ、成功時のみカルテ入力が活性化する |
  | 1-C リワード実績ダッシュボード | 本部・統括 | 交換→予約→来店→当日購買のファネル、ポイント負債(引当)の消化インパクト、体験者 vs 非体験者の30日以内再来店率、フォローアップ配信の開封・タップ、店舗別消化率と未消化アラート |

  ### 2. パートナー共創型(発展形) — MASH ALLIANCE CLOUD
  企画例: Cosme Kitchen × gelato pique × MOOMIN「ムーミン谷のボタニカル・アトリエ」

  | 画面 | 対象ロール | 内容 |
  | --- | --- | --- |
  | 2-A 共創アトリエ・スタジオ | アライアンス統括 | 提携パートナー・共同主催ブランド(2つ以上でクロスユース計測が自動有効化)/ IPライセンス＆ガバナンス(契約許諾期間、契約終了時オートメーション、監修ステータスとアセット別の承認状況、ロイヤリティとMG消化率)/ Points + Cash と先行予約枠。右カラムの**時計デモ**(公開前・先行期間・一般公開中・契約終了後)で特設ページの表示が自動的に切り替わり、**監修が1件でも未承認になると公開ボタンと特設ページがロックされる** |
  | 2-B 拠点スロット＆IP資材 | 店舗・資材マネージャー | スロット長×ブース数から1日枠数を自動算出、ブース×時間帯のスロット表。キャラクター別のIP限定資材を**パートナー寄託在庫**として追跡し、残少アラート → WEB予約の選択肢を自動非表示 → 完売の状態遷移をデモ操作で確認できる。ベース資材の残量・補充ステータス、返却対象数量の精算サマリー付き |
  | 2-C アトリエ接客コンソール | 店頭スタッフ | Points + Cash の決済確認まで含むチケット認証(現金未受領・枠時間超過のエラー表示あり)、マインド＆アロマ診断(**2-Bの在庫と連動し、完売キャラクターは選択不可**)、資材シリアルQRの読取バリデーション(形式不一致・キャラクター不一致)、gelato pique 連動提案、30日後リフィルの自動予約権付与 |
  | 2-D アライアンスROI／相互インサイト | 共創パートナー還元 | パートナー還元データ(体験者数・キャラクター選択シェア・参加者属性・NPS・自由記述)とMASHグループ内の事業インパクト(クロスユース率・客単価リフト・リフィル再来店率・引当ポイント消化)。**パートナー提出モード**をONにするとグループ内指標がロックオーバーレイで覆われ、PDF出力の収録範囲もそれに追随する。自社完結型との KPI 比較、ロイヤリティ精算サマリー、次期共創に向けた示唆も収録 |

  ### リファレンス: ステータス定義
  Figma等でのワイヤーフレーム化を想定し、各コンポーネントの状態(正常 / 残少 / 在庫ゼロ / 期限切れ / 監修待ちロック / 権限外)、フォームのエラー時表示、認証・消込の結果表示、在庫残数のしきい値設計(残数に応じてWEB予約・店頭カルテ・通知がどう変わるか)を一覧化した画面を用意している。

## コード構成(共通更新の仕組み)

3系統に共通するロジック・スタイル・データ(状態管理、QR決済フロー、リワード交換フロー、チャレンジ、履歴、プッシュ通知、共通ポイント交換ロゴ、写真素材など)は `assets/core.css` と `assets/core.js` に集約している。フル版だけが使うモード切替・ガチャ・キャラクター演出・ストーリーゲージ・桜演出は `assets/theme-switch.css` / `assets/theme-switch.js` に分離し、`index.html` だけが読み込む。

```text
assets/
  core.css           全ビルド共通のスタイル
  core.js            全ビルド共通のロジック・データ・initApp()
  theme-switch.css    フル版のみ: モード切替/ガチャ/キャラクター演出のスタイル
  theme-switch.js     フル版のみ: 同上のロジック・データ(THEMES.sports/wakaoshi 等を core の
                       オブジェクトに Object.assign で追加し、renderThemeChrome 等の一部関数は
                       テーマ対応版で再定義して core の簡易版を上書きする)
index.html    core + theme-switch を読み込む薄いページ(モード切替+下部ナビの構成)
lite.html     core のみを読み込む薄いページ(下部ナビの構成)
hub.html      core のみを読み込む薄いページ + ページ内だけの追加CSS/JS(ハブナビの構成)
build.py      thin wrapperを単一HTMLファイルに束ねるビルドスクリプト(下記参照)
```

**共通ロジックを直したいとき**は `assets/core.css` / `assets/core.js` を1回編集すれば、3ビルドすべてに反映される(ブラウザは `<link>` / `<script src>` で毎回最新のファイルを読み込むため、GitHub Pages 上では commit → push するだけでよい)。フル版だけの見た目・挙動(モード切替やガチャなど)を直したいときは `assets/theme-switch.*` を編集する。各ページ固有のHTML構成(下部ナビ vs ハブナビなど)は各 `.html` ファイル自体を編集する。

### Claude Artifact 用に単一ファイルへ束ねる

Claude Artifact は1ファイルしか受け付けないため、上記の分割ファイルをその場で1ファイルに結合するスクリプトを用意している。

```bash
python3 build.py index.html          # index.standalone.html を生成
python3 build.py lite.html
python3 build.py hub.html
python3 build.py --all /path/to/dir  # 3つまとめて生成
```

`assets/core.*` や `assets/theme-switch.*` を更新したら、Artifactとして配布している版を最新化するために `build.py` を実行し、生成された `*.standalone.html` をArtifactとして再公開する。

## デモ公開(GitHub Pages)

このリポジトリを GitHub にプッシュし、Pages を有効化すると `https://<ユーザー名>.github.io/<リポジトリ名>/` で複数人にURLを配布してデモできる(サブパスの `/lite.html` `/hub.html` も同様)。

```bash
# 1. GitHub上にリポジトリを作成(Web UIまたは gh CLI)
gh repo create <リポジトリ名> --public --source=. --remote=origin --push
# もしくは、Web UIで空のリポジトリを作成した後:
# git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
# git push -u origin main

# 2. GitHub Pages を有効化
# リポジトリの Settings > Pages > Source を「Deploy from a branch」、
# Branch を「main」/「/(root)」に設定して保存。
# 数分後に https://<ユーザー名>.github.io/<リポジトリ名>/ で公開される。
```

## 注意事項

- 「わかおしっ！」モードのキャラクターイラストは和歌山市の公式PRキャラクターの画像を使用している(`assets/theme-switch.js` のみに含まれ、`lite.html`/`hub.html` には一切含まれない)。社外・商用利用には和歌山市への事前相談と利用許諾申請(無償・2年以内)が必要。本プロトタイプはあくまで内部検討用。
- `mash.html` は特定企業(株式会社マッシュホールディングス)への提案を想定した架空の企画プロトタイプ。登場するブランド名・パートナー名(MOOMIN / Moomin Characters 等)・商品名・数値はすべて提案検討用のダミーであり、各社の実データでも、許諾を得た使用でもない。社外提示の際は事前に権利関係を確認すること。デザインはマッシュグループのコーポレートサイト(mashgroup.jp)のエディトリアルな世界観を参考にした独自実装で、同社のデザインアセットは使用していない。
- リワードの「共通ポイント交換」(Vポイント/dポイント/PayPayポイント/楽天ポイント)のロゴは、実物ロゴを模した独自デザイン(色・形状のみ参考)であり、各社の公式ロゴデータそのものではない。
- GitHub Pages は無料プランの場合、公開リポジトリでのみ利用可能(=サイトは非公開URLではあるがインターネット上に公開される)。社外に見せたくない情報が含まれていないか確認のうえ公開すること。
