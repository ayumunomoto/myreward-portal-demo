# マイリワードポータル プロトタイプ

外部ライブラリを使わない静的HTMLのプロトタイプ群。ビルド工程はない。

## 公開・共有

**共有URLの正は Vercel。** 本番は https://myreward-portal-demo.vercel.app
（`/lite` `/hub` `/admin` `/dashboard`）。`main` への push で反映される。
プロジェクト画面に出る `...-<ハッシュ>-<スコープ>.vercel.app` はデプロイ単位の固定URLなので配らない。

**Claude Artifact への公開・再公開はしない。** 更新のたびに公開済みの全文を
読み直す必要があり、1回の更新にかかるコストが大きいため運用から外した。
既存の Artifact は 2026-09 時点の凍結スナップショットとして残っているだけで、
更新しない。共有を頼まれたら Vercel のURLを案内する。

`build.py` は `assets/` を相対参照できない配布先向けのバンドラ。
通常の更新では実行しない。

## 見た目の確認

ブラウザで直接開く（`dashboard.html` と `admin.html` は単一ファイルなので `file://` で開ける。
`index.html` 系は `assets/` を参照するので `python3 -m http.server` 経由）。
スクリーンショットを撮って確認するのは、こちらが目視で判断する必要があるときだけにする。

## ファイル

| ファイル | 内容 |
| --- | --- |
| `index.html` / `lite.html` / `hub.html` | 消費者向けアプリの3ビルド。`assets/core.*` と `assets/theme-switch.*` を共有 |
| `admin.html` | 管理画面(CMS)。単一ファイル。上記とコードを共有しない |
| `dashboard.html` | 効果測定ダッシュボード。単一ファイル・約3,500行。CSS/JSインライン |

`dashboard.html` は大きいので、編集は該当箇所を特定してから行う。全文の読み込みは避ける。
チャートはすべてインラインSVGの自前描画で、チャートライブラリは使わない。
デザインは東京都デジタルサービス局「ダッシュボード作成ガイドブック」のパレットとフォントサイズに準拠。

## 制約

- 数値はすべてダミー値。実顧客データは置かない
- 集計値のみを表示する。会員IDを出すのは不正検知画面だけ（個人特定を避けるため）
- 作業はフィーチャーブランチで行い、`main` へは許可なく push しない

## 参照

- `docs/dashboard-requirements.md` — ダッシュボードの要件・指標定義・画面要件
- `docs/point-campaign-benchmark.md` — 他行ポイント施策のベンチマークと打ち手カタログ
